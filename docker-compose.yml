version: '3.8'

services:
  discord-bot:
    build: .
    container_name: discord-bot
    restart: always
    env_file:
      - .env
    environment:
      - BOT_ENV="production"
    working_dir: /app
    command: python bot.py
