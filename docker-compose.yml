version: '3.8'

services:
  discord-bot:
    build:
      context: https://github.com/RealBeastman/pokeInfo.git#production
    container_name: discord-bot
    restart: always
    env_file:
      - .env
    working_dir: /app
    command: python bot.py
